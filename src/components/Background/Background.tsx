import * as React from 'react';
import {
  BackgroundStyled,
} from './styles';

const Background : React.StatelessComponent<any> = ( props : any ) => {
  return (
    <BackgroundStyled />
  );
};

export default Background;
